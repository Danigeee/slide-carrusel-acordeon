$('.xxx-sliders-container').slick({
    slidesToShow: 2,
    prevArrow: $('.prev'),
    nextArrow: $('.next'),
    initialSlide:1,
    // para que se vea un poquito a la izquierda
    variableWidth: true,

    centerMode: true,
})